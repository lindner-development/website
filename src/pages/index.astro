---
if (Astro.request.headers.has("Accept-Language")) {
  let lang = Astro.request.headers.get("Accept-Language")?.toLowerCase();
  if (lang) {
    if (lang.includes("de")) {
      return Astro.redirect("/de/", 301);
    }
  }
}

return Astro.redirect("/en/", 301);
---
